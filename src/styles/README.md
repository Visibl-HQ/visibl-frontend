# Styles

Global design tokens live in `src/app/globals.css` because Tailwind v4 reads CSS-first theme variables there. Shared class conventions live in `src/config/tokens.ts` so layout and typography choices stay centralized without creating a second styling system.
